{
  "name": "apollo-link-cache",
  "version": "0.1.0",
  "description": "Apollo link to access cache",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel ./src --out-dir ./lib",
    "watch": "babel ./src --watch --out-dir ./lib"
  },
  "author": "Kiran Abburi",
  "license": "MIT",
  "dependencies": {
    "apollo-link": "^1.2.1",
    "apollo-utilities": "^1.0.8",
    "graphql-anywhere": "^4.1.5",
    "lodash": "^4.17.5"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "babel-cli": "^6.26.0"
  }
}
